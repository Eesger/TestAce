services:
  archiver:
    build: .
    restart: unless-stopped
    env_file: .env          # contains all API keys + config
    volumes:
      - ./data:/data        # persists bookmarks.db between runs
    environment:
      - DB_PATH=/data/bookmarks.db
